spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stockplatform?useSSL=false&serverTimezone=UTC
    username: root
    password: Mannu@8453
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true

server:
  port: 8080
